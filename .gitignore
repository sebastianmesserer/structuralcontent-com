.DS_Store

# Strategy docs — never publish (repo is public)
References/

# Local scratch
examples-anim-preview.html

# Worker: the system prompt is IP — bundled into the Worker at deploy time, never committed
worker/prompts/system-prompt.md
worker/node_modules/
worker/.wrangler/
worker/.dev.vars
